import { Box } from "@mui/material";

const SearchResult = () => {
  return <Box sx={{ position: "fixed", top: 64, zIndex: 30, minHeight: 150, width: "100%", bgcolor: "red" }}>SearchResult</Box>;
};

export default SearchResult;
